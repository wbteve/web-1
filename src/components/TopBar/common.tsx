import { Pending, Fulfilled, Rejected } from '../../utils/asyncActionGenerator'
import { hashHistory } from 'react-router'

export const ActionType = {
}

export const ActionGenerators = {

}

export interface TopBarState {
    location?:any
}
